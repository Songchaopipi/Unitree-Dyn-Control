# Hierarchical Reduced-Order MPC 与力矩控制器整理

本文档总结论文“Hierarchical Reduced-Order Model Predictive Control for Robust Locomotion on Humanoid Robots”的两层控制结构（高层 NMPC 与中层线性 MPC）以及底层力矩/关节映射实现，便于在工程中参考实现与调试。

## 1 概览
- 架构为分层控制：高层（Step-to-Step NMPC）基于 ALIP（Angular LIP）S2S 动力学进行步长、步时与踝力矩联合优化；中层为线性化的单刚体（SRB）MPC 的扩展（本文称为 DSRB），将上肢/躯干（臂与躯干）作为子刚体纳入线性问题中；底层负责将中层输出映射为逆解和力矩前馈并发送到机器人。
- 关键点：高层解决非凸的步时/步长问题（40 Hz），中层保持二次规划（QP）形式以保证实时性（500 Hz），底层以雅可比转置把期望力/力矩映射为关节力矩。

## 2 高层：ALIP 基于 Step-to-Step 的 NMPC
### 2.1 模型与状态
- ALIP 状态：$x_{ALIP}=[p_x\\, L_y\\, p_y\\, L_x]^\\top$，其中 $p_{x,y}$ 为相对于支撑足的 COM 水平位置，$L_{y,x}$ 为绕接触点的角动量。
- 连续单步动力学（带踝力矩 $\\tau=[\\tau_y,\\tau_x]^\\top$）：$\\dot x_{ALIP}=A_{ALIP}x_{ALIP}+B_{ALIP}\\tau$。对单步时间 $T$ 的状态转移矩阵为 $\\Phi(T)=e^{A_{ALIP}T}$，控制影响为 $\\Gamma(T)=\\int_0^T e^{A_{ALIP}(T-s)}B_{ALIP}ds$。

### 2.2 步间（S2S）动力学
- 通过冲击/步移关系，得到离散 S2S 动力学：
$$x_{k+1}=\\Phi(T_k)x_k+\\Gamma(T_k)\\tau_k+B_d\\ell_k$$
其中 $\\ell_k=[\\ell_x\\,\\ell_y]^\\top$ 是步长，$B_d$ 是平移映射。

### 2.3 优化变量与约束
- 优化变量：$\\{x_{ALIP,k},\\;\\ell_k,\\;T_k,\\;\\tau_k\\}_{k=0}^{K-1}$。
- 目标：对偏差项二次惩罚（位置/动量偏差、步长偏差、步时偏差、踝力矩能量），形式为二次目标（但对 $T_k$ 引入非线性）。
- 约束：S2S 动力学（非线性因 $T_k$）、变量上下界（步长、步时、踝力矩、ALIP 状态限幅）、初始条件基于当前步相位（考虑已过时间 $t_{curr}$）。

### 2.4 实现要点
- 求解器：论文采用 CasADi C++ 接口 + IPOPT，在 onboard mini-PC 上以 40 Hz 运行。
- 参考生成：利用 HLIP 周期轨道求取期望前/横向参考，用 (17)-(20) 将 HLIP 引入 ALIP 目标。

## 3 中层：分解的 SRB 线性 MPC（DSRB）
### 3.1 动机
- 传统 SRB-MPC 只考虑质心与外接触力/力矩，不显式建模上肢和躯干。为增强对偏航与上半身扰动的拒抗能力，论文将机器人分为下躯干（LB）、躯干（TR）与左右手臂（LA/RA）四部分，允许手臂位置/加速度和躯干 $\\psi_{TR}$ 产生内部力矩。

### 3.2 DSRB 状态与输入
- 状态：$x_{DSRB}=h(x_{SRB})^\\top,\\;\\psi_{TR},\\;p_{LA},\\;p_{RA},\\;\\dot\\psi_{TR},\\;v_{LA},\\;v_{RA}$（将 SRB 展开并加上手臂与躯干自由度）。
- 输入：$u_{DSRB}=[u_{SRB}^\\top,\\;\\tau_{TR},\\;F_{LA,x},\\;F_{RA,x}]^\\top$，其中 $u_{SRB}$ 为左右足 GRF/GRM，$\\tau_{TR}$ 为躯干 z 轴执行器对躯干的扭矩，$F_{LA,x},F_{RA,x}$ 为限定方向的臂力（仅沿 x 方向移动以保持线性）。

### 3.3 线性化与离散化
- 在每个 MPC 步节点围绕参考轨迹线性化 SRB/DSRB 动力学，得到离散线性模型：
$$x_{n+1}=A_n x_n+B_n u_n$$
- 保持约束线性（摩擦锥、力限制、摆动脚为零力矩等），于是 MPC 成为标准 QP：二次目标 + 线性等式/不等式约束。

### 3.4 臂与躯干的线性化技巧
- 为保持线性，限制手臂仅在 x 轴移动，使得臂产生的力矩关于输入呈线性（见 (30)）。躯干仅允许 z 轴转动且可通过执行器施加 $\\tau_{TR}$，相应地在 B 矩阵中用常数系数表示。

## 4 底层：关节映射与力矩前馈
### 4.1 轨迹生成
- 将 S2S（高层）产生的步次/步时转成 SRB 参考轨迹，并在节点间通过流动/反向流动 ALIP 动力学得到任意时刻的 COM 参考（参见 (24)-(27)）。
- 摆动足轨迹使用光滑接合法（论文引用 [23]）以确保平滑性并满足位姿/速度边界。

### 4.2 力到关节的映射
- 使用质心相对于关节的雅可比矩阵，将 MPC 输出的期望接触力/内部力矩映射为关节力矩的前馈项：
$$q_{cmd}^\\tau = J^\\top u_{DSRB,0}$$
其中 $u_{DSRB,0}$ 为 MPC 在当前步的第一个控制量。

### 4.3 逆运动学与位置指令
- 位置指令 $q_{cmd}^p$ 通过逆运动学（Pinocchio）基于当前状态与 DSRB 轨迹计算得到，用于 PD/位置控制环。

## 5 实验/性能要点
- 控制频率：高层 NMPC 40 Hz；中层与低层 500 Hz。
- 求解器/库：CasADi + IPOPT（高层 NLP）；QP 解算器（中层 QP）；Eigen 与 Pinocchio 用于线性代数与运动学。
- 结果摘要：自适应步时使推挤恢复成功率提升约 36%；引入上半身控制（DSRB）能明显提升偏航扰动拒抗能力；在多种室内外地形与硬件上验证（Unitree G1）。

## 6 实现建议与注意事项
- 初始化：高层 NMPC 的初始条件须基于当前步相位（考虑 t_curr）以避免步时变量与当前步冲突。
- 线性化频率：中层需要快速、确定的求解时长以保证 500 Hz，故保持问题为 QP 并限制非线性项（例如手臂运动轴向限制）。
- 模型不匹配：ALIP / DSRB 假设（固定高度、小角度、点质量或简化惯量）在真实机器人上会被肢体惯性违反，需通过 Q 与约束设计并辅以观测器/滤波（strap-down INS）减缓误差影响。

## 7 参考代码位置（工程内提示）
- 论文中提到的实现细节可映射到工程中的：使用 CasADi C++（高层）、MPC QP（中层）、Pinocchio（运动学），在本工程可参考 `algorithm/` 下的 MPC 与 wbc 文件实现（探索并对接时可参照已有模块）。

---
本文件由论文主要内容整理，聚焦于工程实现层面的模块划分、关键公式与实现要点。如需我把高层 NMPC 用 CasADi C++ 快速生成一个模板（含变量/约束形式），或把 DSRB 的离散化矩阵生成脚本写入工程，请告诉我下一步偏好。
